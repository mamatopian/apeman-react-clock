/**
 * Test case for apDigitalClockNumberBar.
 * Runs with mocha.
 */
"use strict";

const apDigitalClockNumberBar = require('../lib/ap_digital_clock_number_bar.js'),
    assert = require('assert');

describe('ap-digital-clock-number-bar', () => {

    before((done) => {
        done();
    });

    after((done) => {
        done();
    });


    it('Ap digital clock number bar', (done) => {
        done();
    });
});


/**
 * Test case for fonts.
 * Runs with mocha.
 */
'use strict'

const fonts = require('../lib/fonts.js')
const assert = require('assert');

describe('fonts', () => {
  before((done) => {
    done()
  })

  after((done) => {
    done()
  })

  it('Fonts', (done) => {
    done()
  })
})

/* global describe, before, after, it */
